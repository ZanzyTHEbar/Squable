import { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
    appId: 'com.prometheontechnologies.squable',
    appName: 'Squable',
    webDir: 'dist',
    bundledWebRuntime: false,
}

export default config
